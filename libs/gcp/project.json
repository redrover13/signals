{
  "name": "gcp",
  "sourceRoot": "libs/gcp/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'gcp lib build (no-op)'"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'lint placeholder'"
      }
    }
  }
}
