{
  "name": "agents-lib",
  "sourceRoot": "libs/agents/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'agents lib build (no-op)'"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'lint placeholder'"
      }
    }
  }
}